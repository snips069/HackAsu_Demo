// Add integration tests for utility layer
describe('Auth Utils', () => {
  it('should normalize errors correctly', () => {
    // Test implementation
  });
});

// Stabilize flaky session tests
describe('Session Tests', () => {
  it('should handle session timeouts', () => {
    // Test implementation
  });
});